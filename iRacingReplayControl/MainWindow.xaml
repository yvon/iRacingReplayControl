<mah:MetroWindow x:Class="iRacingReplayControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:iRacingReplayControl"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        mc:Ignorable="d"
        Closed="MetroWindow_Closed"
        Title="Replay" SizeToContent="WidthAndHeight">
    <StackPanel Margin="0 5">
        <Button Content="Add current camera" Click="AddCam" Height="28" Margin="5"/>
        <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="800">
            <ItemsControl x:Name="itemsControl" Margin="2 5" ItemsSource="{Binding}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <DataTemplate.Resources>
                            <Style TargetType="Button">
                                <Setter Property="OverridesDefaultStyle" Value="true" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition></ColumnDefinition>
                                                    <ColumnDefinition></ColumnDefinition>
                                                    <ColumnDefinition></ColumnDefinition>
                                                </Grid.ColumnDefinitions>
                                                <Label Content="{Binding Time}"/>
                                                <Label Content="{Binding CarIdx}" Grid.Column="1" ContentStringFormat="#{0}"/>
                                                <Label Content="{Binding CamNumber}" Grid.Column="2" ContentStringFormat="Grp {0}"/>
                                            </Grid>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataTemplate.Resources>
                        <Button Click="ClickOnCam" MouseRightButtonDown="ClickOnDeleteCam" Cursor="Hand"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Label FontSize="10" Content="Current cam" FontWeight="Bold"/>
            <Label FontSize="10" Content="{Binding Time}"/>
            <Label FontSize="10" Content="{Binding CarIdx}" ContentStringFormat="Car #{0}"/>
            <Label FontSize="10" Content="{Binding CamNumber}" ContentStringFormat="Grp {0}"/>
        </StackPanel>
    </StackPanel>
</mah:MetroWindow>
